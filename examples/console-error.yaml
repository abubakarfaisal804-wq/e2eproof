version: 1
name: Detect console error
base_url: http://127.0.0.1:8765
variables: {email: "console+{{run_id}}@example.com"}
policy:
  allowed_hosts: [127.0.0.1, localhost]
  timeout_ms: 5000
  fail_on_console_error: true
  fail_on_page_error: true
  fail_on_request_failure: true
evidence: {output_dir: evidence, screenshot: failure, trace: failure}
flows:
  - id: console-gate
    claim: The user flow completes without browser console errors.
    steps:
      - {type: browser.goto, url: /app/console-error}
      - {type: browser.fill, target: {label: Email}, value: "{{email}}"}
      - {type: browser.click, target: {role: button, name: Submit lead}}
